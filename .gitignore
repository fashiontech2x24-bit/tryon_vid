# runtime
logs/
*.pid
*.log

# python
__pycache__/
*.pyc
.venv/
venv/

# local scratch / large media
*.mp4
*.webm
*.mov
*.png
*.jpg
*.jpeg
!web/**/*.png
localtest/

# pipeline runtime artifacts (do not commit)
runs/
.pose_cache/
models/

# but DO commit the bundled motion presets despite the *.mp4 rule above
# (+ their retarget-config sidecars from the control workbench)
!assets/motion_presets/
!assets/motion_presets/*.mp4
!assets/motion_presets/*.json

# uploaded control-source recordings (workbench input; large, per-instance)
assets/control_sources/
